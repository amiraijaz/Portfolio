# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini

# Local-only archive (old drafts, original full-res photo, dev tooling).
# Kept on your machine, not pushed to the public repo or deployed.
_archive/

# Node tooling, if ever added
node_modules/
